-- Local PostgreSQL state: {domain or schema name}
-- Purpose: initialize a fresh local Docker volume to the current approved development state
-- Release counterpart: gonna-deploy compares this local state with deploy/sql/postgres/ when the user requests an online migration

-- {schema and baseline data statements required by local development}
